import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

function Cart(isAdding, isUpdating, isDeleting) {
  const navigate = useNavigate();

  return <div></div>;
}

export default Cart;
